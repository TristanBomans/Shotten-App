# Agent notes

The product is the PWA in `pwa/`. Work there unless the user names another path.

`mobile/` is a deprecated Expo/React Native app. Do not read, edit, test, or propose changes in `mobile/` unless the user explicitly asks about that frozen native app.
